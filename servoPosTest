#include <Servo.h>
#define LED 13
void setup() {
  // put your setup code here, to run once:
  Servo myservo;
  myservo.attach(9);
  myservo.write(60);
  pinMode(LED, OUTPUT);
  digitalWrite(LED,HIGH); 
  
}

void loop() {
  // put your main code here, to run repeatedly:
  
}
